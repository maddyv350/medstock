# Backend API base URL used by the admin panel (dev + build).
# Points at the hosted Render backend. For local dev against a local backend,
# comment this out (the Vite dev server then proxies /api to localhost:4000).
VITE_API_URL=https://medstock-backend-sddo.onrender.com/api
